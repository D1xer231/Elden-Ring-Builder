<Window x:Class="Elden_Ring_Builder.LoadScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Elden_Ring_Builder"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Topmost="True"
        Title="LoadScreen" Height="400" Width="300">
    <Grid>
        <Border CornerRadius="15" Padding="20" BorderBrush="#047DD3" BorderThickness="2">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF131521"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <Image Source="/img/icons/ico100.png"  Width="100" Height="100"/>

                <TextBlock Text="Elden Ring Builder"
                       FontSize="22"
                       Foreground="#047DD3"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

                <ProgressBar x:Name="ProgressBar"
                         Width="250" Height="15"
                         Foreground="#047DD3"
                         Background="#333333"
                         IsIndeterminate="True"/>

                <TextBlock Text="Hello,"
                       Foreground="White"
                       FontSize="16"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
                
                <TextBlock Text="{Binding UserName}"
                       Foreground="#047DD3"
                       FontSize="20"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,10,0,0"/>
            </StackPanel>
        </Border>
        
    </Grid>
</Window>
