<Window x:Class="Elden_Ring_Builder.RuneInfo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Elden_Ring_Builder"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Style="{StaticResource MaterialDesignWindow}"
        Title="RuneInfo" Height="400" Width="300">
    <Grid MouseLeftButtonDown="Grid_MouseLeftButtonDown">
        <Border CornerRadius="15" Padding="20" BorderBrush="#047DD3" BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="#FF131521"/>
                    <GradientStop Color="Black" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="150"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>

                <TextBlock Text="{Binding name}" Foreground="#FFD700" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" TextWrapping="Wrap"/>
                <Button HorizontalAlignment="Right" VerticalAlignment="Top" Width="20" Height="20" BorderBrush="{x:Null}" Click="close_window_btn_Click">
                    <Button.Background>
                        <ImageBrush ImageSource="/img/icons/exit.png" Stretch="Uniform"/>
                    </Button.Background>
                </Button>
                <Image Source="{Binding image_path}" HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1" />
                <TextBlock Text="{Binding description}" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" Grid.Row="2" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding location}" Foreground="White" FontWeight="Light" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="12" Grid.Row="3" TextWrapping="Wrap"/>
                <StackPanel Grid.Row="4">
                    <TextBlock Text="{Binding effect}" Foreground="Red" VerticalAlignment="Center" TextWrapping="Wrap" HorizontalAlignment="Center" FontSize="12" Margin="0,0,0,5"/>
                    <TextBlock Text="{Binding type}" Foreground="#FFD700" FontWeight="ExtraLight" VerticalAlignment="Center" TextWrapping="Wrap" HorizontalAlignment="Center" FontSize="12" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
