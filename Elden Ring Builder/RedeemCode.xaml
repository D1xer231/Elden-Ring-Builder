<Window x:Class="Elden_Ring_Builder.RedeemCode"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Elden_Ring_Builder"
        mc:Ignorable="d"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        ShowInTaskbar="False"
        Style="{StaticResource MaterialDesignWindow}"
        Title="RedeemCode" Height="400" Width="300">
    <Grid MouseLeftButtonDown="Grid_MouseLeftButtonDown">
        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FF131521"/>
                <GradientStop Color="Black" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        <Border CornerRadius="15" Padding="20" BorderBrush="#047DD3" BorderThickness="2">
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center">
                <StackPanel.Background>
                    <ImageBrush Stretch="Uniform"/>
                </StackPanel.Background>
                <Image Source="/img/icons/ico100.png"  Width="100" Height="100"/>
                <TextBlock Text="Redeem Code"
                       FontSize="22"
                       Foreground="#047DD3"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,20,0,20"/>
                <TextBox x:Name="CodeTextBox"
                         Width="150"
                         Height="35"
                         FontSize="15"
                         Margin="0,0,0,20"
                         HorizontalAlignment="Center"
                         VerticalAlignment="Center"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Foreground="White"/>
                <Button Content="Redeem"
                        Width="100"
                        Height="30"
                        Background="#047DD3"
                        Foreground="White"
                        FontWeight="Bold"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="RedeemButton_Click"/>
                <TextBlock x:Name="redeemed_code" Foreground="White" HorizontalAlignment="Center" Margin="0,10,0,0"/>
                <Button x:Name="clear_btn" 
                        Content="Clear"
                        Width="60"
                        Height="20"
                        Background="#047DD3"
                        Foreground="White"
                        FontSize="10"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Click="clear_btn_Click" Margin="0,5,0,0"
                        />
            </StackPanel>
        </Border>
        <Button VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="30" Height="30" BorderBrush="{x:Null}" Margin="0,0,10,10" Click="send_email_btn_Click" Cursor="Hand">
            <Button.Background>
                <ImageBrush ImageSource="/img/icons/info.png" Stretch="Uniform"/>
            </Button.Background>
        </Button>
        <Button VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,10,10,0" BorderBrush="{x:Null}" Cursor="Hand" Click="exit_btn_Click" Width="30" Height="30">
            <Button.Background>
                <ImageBrush Stretch="Uniform" ImageSource="/img/icons/exit.png"/>
            </Button.Background>
        </Button>
    </Grid>
</Window>
